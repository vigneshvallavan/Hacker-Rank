ANSWER:

n=list(map(int,input().split()))
count=1
l=[]
if len(n)==1:
    count=1
    print(count)
else:
    for t in range(0,len(n)-1,1):
        if n[t]<n[t+1]:
            count+=1
        else:
            l.append(count)
            count=1
    print(max(l))
